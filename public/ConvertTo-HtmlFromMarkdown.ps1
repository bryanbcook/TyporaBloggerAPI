function ConvertTo-HtmlFromMarkdown
{
    param(
        [Parameter(Mandatory=$true, HelpMessage="Path to Markdown file")]
        [string]$File
    )

    # Test if the markdown file exists

    # Use pandoc to convert the markdown to Html


    # Apply additional transforms
    
}